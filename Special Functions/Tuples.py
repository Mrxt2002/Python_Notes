# Tuples and the 'count' method to return the count of a particular object
numbers = (1, 2, 3, 4, 4, 4, 4, 4, 4, 4, 4)

# Use the 'count' method of the tuple to return the count of the object '4'
count_of_4 = numbers.count(4)

# Print the count
print(count_of_4)
